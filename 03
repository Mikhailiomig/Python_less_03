# # Задайте список из нескольких чисел. Напишите программу, которая найдёт сумму 
# # элементов списка, стоящих на нечётной идексах.

n = int (input ('Сколько элементов вы будете вводить: '))
a = [0] * n
for i in range (n):
    a[i] = int (input ()) 
print ('Ваш список: ')
print (a, end = ' ')
print ()
print ('Сумма элементов с нечетным индексом: ', sum (a[1::2]))





# Напишите программу, которая найдёт произведение пар чисел списка. 
# Парой считаем первый и последний элемент, второй и предпоследний и т.д.

n = int (input ('Сколько элементов вы будете вводить: '))
a = [0] * n
for i in range (n):
    a[i] = int (input ()) 
print ('Ваш список: ')
print (a, end = ' ')
print ()
print ('Произведение пар чисел: ')
j = 1
b = []
for i in range (len (a) // 2 + 1):
    pro = a[i] * a[i -j]
    b.append(pro)
    j = j + 2
print (*b)





# # Задайте список из вещественных чисел. Напишите программу, которая найдёт разницу 
# # между максимальным и минимальным значением дробной части элементов.

n = int (input ('Сколько элементов вы будете вводить: '))
a = [0] * n
for i in range (n):
    a[i] = float (input ()) 
print ('Ваш список: ')
print (a, end = ' ')
b = []
for i in range (len (a)):
    k = a[i] % 1
    b.append (round (k, 2))
print ()
print ('Разница между max и min дробными частями: ', max (b) - min (b))






# # Напишите программу, которая будет преобразовывать десятичное число в двоичное.

a = ''
n = int(input("Введите число :\n"))
while n != 0:
    a =  str (n % 2) + a
    n //= 2
print ('Это число в двоичнов виде: ')   
print (int (a))





# # Задайте число. Составьте список чисел Фибоначчи, в том числе для отрицательных индексов.

n = int(input('Введите число: '))
def fibo(n):
    f_list = []
    a, b = 1, 1
    for i in range(n-1):
        f_list.append(a)
        a, b = b, a + b
    a, b = 0, 1
    for i in range (n):
        f_list.insert(0, a)
        a, b = b, a - b
    return f_list
f_list = fibo(n)
print(fibo(n))



